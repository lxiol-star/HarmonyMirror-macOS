# HarmonyMirror (简化版)

<div align="center">

**macOS 上的鸿蒙设备投屏工具**

在 Mac 上实时查看和控制 HarmonyOS 手机/平板

[功能特性](#功能特性) • [快速开始](#快速开始) • [使用方法](#使用方法) • [技术实现](#技术实现) • [文档](#项目文档)

</div>

---

## 项目简介

HarmonyMirror 是一个 macOS 原生应用,让你在 Mac 上实时查看和控制鸿蒙手机/平板的屏幕。类似于 Android 的 scrcpy,但专为 HarmonyOS 设备优化。

**本分支为简化版本**,仅使用 hdc 命令进行输入控制,无需部署设备端 Agent。适合快速上手和日常使用。

### 核心特性

- **低延迟投屏**: 60fps H.264 硬件解码,流畅的实时画面
- **简单可靠**: 纯 hdc 输入,无需额外部署
- **无线连接**: 支持 USB 和 Wi-Fi 连接,自动发现局域网设备
- **原生体验**: Swift/SwiftUI 构建,完美适配 macOS

### 与完整版的区别

| 特性 | 简化版 (no-agent) | 完整版 (main) |
|------|------------------|---------------|
| 输入延迟 | ~100ms (hdc shell) | <2ms (HarmonyAgent) |
| 部署复杂度 | 低 (无需额外部署) | 中 (需部署 Agent) |
| 稳定性 | 高 (纯 hdc) | 高 (自动回退) |
| 适用场景 | 日常使用 | 游戏/高频操作 |

## 功能特性

### 连接与发现
- ✅ USB / Wi-Fi 自动发现和连接鸿蒙设备
- ✅ 局域网设备自动扫描
- ✅ 设备友好名称显示（如 "HUAWEI Mate 70 Pro+"）
- ✅ Wi-Fi 连接可靠性优化（自动检测端口变化）
- ✅ Wi-Fi 网络诊断（Mac IP、网关、目标端口探测）

### 投屏与显示
- ✅ 实时屏幕镜像（60fps H.264 硬件解码）
- ✅ 横竖屏自动切换
- ✅ 手机 / 平板自动识别和适配
- ✅ 连接等待动画和状态提示
- ✅ macOS 触控板双指滚动和捏合缩放

### 输入控制
- ✅ 触摸/滑动/长按输入控制（hdc uinput）
- ✅ 远程唤醒屏幕
- ✅ 通知中心/控制中心快捷按钮

### 开发与测试
- ✅ 自动化测试启动参数（指定设备自动连接、定时退出）
- ✅ 详细的日志和调试信息

## 快速开始

### 系统要求

#### Mac 端
- macOS 13.0+ (测试环境: macOS 26.4.1)
- Xcode 15.0+
- [DevEco Studio](https://developer.huawei.com) 或 [DevEco Testing](https://developer.huawei.com)（提供 hdc 工具）
- Python 3.9+（含 grpcio、protobuf 依赖）

#### 设备端
- HarmonyOS 4.0+ (测试设备: HUAWEI Mate 70 Pro+, MatePad Pro)
- 开启开发者模式和 USB 调试
- 首次使用需先通过 DevEco Testing 打开一次投屏（安装 `libscreen_casting.z.so`）

### 构建

```shell
xcodebuild -project HarmonyMirror.xcodeproj -scheme HarmonyMirror -configuration Debug build
```

### 运行

```shell
open ~/Library/Developer/Xcode/DerivedData/HarmonyMirror-*/Build/Products/Debug/HarmonyMirror.app
```

> 注意：Debug 构建必须通过 `open` 启动 `.app`，不能直接执行二进制文件。

### 自动化测试启动

```shell
# 连接指定设备
open -n /Applications/HarmonyMirror.app --args --connect 192.168.1.100:10178 --exit-after 30

# 自动连接首个可用设备
open -n /Applications/HarmonyMirror.app --args --connect-first --exit-after 30
```

- `--connect <serial>`：自动连接指定 hdc target。
- `--connect-first`：自动连接首个可用设备，优先 USB，其次平板。
- `--exit-after <seconds>`：连接后定时退出，便于 smoke test。

## 使用方法

### USB 连接（推荐）

1. USB 线连接设备和 Mac
2. App 自动发现设备，点击"连接手机/平板"

### Wi-Fi 连接

1. 确保 USB 已连接且设备开启无线调试
2. 点击"开启无线调试"按钮
3. 拔掉 USB，在输入框填入设备 IP（如 `192.168.1.100:10178`）
4. 点击"连接 Wi-Fi"

App 也会自动扫描局域网并连接发现的设备。

### 投屏操作

| 操作 | 方式 |
|------|------|
| 点击 | 在画面上点击 |
| 拖拽 | 按住并移动 |
| 长按 | 按住不放 (>150ms) |
| Back | 工具栏 Back 按钮，或右键点击画面 |
| Home | 工具栏 Home 按钮 |
| 唤醒屏幕 | 工具栏太阳图标 |
| 通知中心 | 工具栏铃铛图标 |
| 控制中心 | 工具栏滑块图标 |
| 双指滑动 | 触控板双指滑动，映射为设备内滚动 |
| 捏合缩放 | 触控板两指张开/捏合，缩放投屏窗口 |
| 断开 | 工具栏"断开"按钮 |

### 注意事项

- 锁屏和密码输入界面因系统安全限制无法远程操作，需在设备上手动解锁
- 通知中心/控制中心按钮已改用 `uitest uiInput swipe`；桌面状态可打开，锁屏状态仍受系统安全限制
- Wi-Fi 连接需要设备和 Mac 在同一局域网，部分路由器可能隔离客户端

## 技术实现

### 架构概览

```
┌──────────────────────────────────────────────────────────────────┐
│                        macOS                                      │
│                                                                    │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │              HarmonyMirror App (Swift/SwiftUI)              │  │
│  │                                                             │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐  │  │
│  │  │ 设备发现      │  │ 投屏服务      │  │ 用户界面         │  │  │
│  │  │ DeviceDiscovery│ │ MirrorService│  │ MirrorWindow    │  │  │
│  │  └──────────────┘  └──────────────┘  └──────────────────┘  │  │
│  │         │                  │                    │            │  │
│  │         ▼                  ▼                    ▼            │  │
│  │  ┌──────────────────────────────────────────────────────┐   │  │
│  │  │              HDCCommand (hdc 命令封装)                │   │  │
│  │  └──────────────────────────────────────────────────────┘   │  │
│  └─────────────────────────────────────────────────────────────┘  │
│                              │                                     │
│                    hdc 命令行工具                                   │
│                              │                                     │
│  ┌───────────────────┐      │      ┌─────────────────────────┐   │
│  │ Python Bridge      │◄─────┘      │ TCPStreamReceiver      │   │
│  │ deveco_cast_bridge │──────────►  │ 接收 H.264 视频帧      │   │
│  │ gRPC → TCP 转发    │   本地TCP   │                         │   │
│  └───────────────────┘              └──────────┬──────────────┘   │
│                                                 │                  │
│                                    ┌────────────▼──────────────┐   │
│                                    │ H264VideoLayer            │   │
│                                    │ 解析 H.264 → 硬件解码显示  │   │
│                                    └───────────────────────────┘   │
└──────────────────────────────────────────────────────────────────┘
                               │
                    USB 数据线 或 Wi-Fi 网络
                               │
┌──────────────────────────────────────────────────────────────────┐
│                     鸿蒙手机 / 平板                                │
│                                                                    │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │  libscreen_casting.z.so (华为投屏扩展)                      │  │
│  │  录屏 → H.264 编码 → gRPC 服务                              │  │
│  └─────────────────────────────────────────────────────────────┘  │
│                                                                    │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │  hdc daemon (设备端守护进程)                                 │  │
│  │  接收 Mac 发来的 shell 命令、触摸注入、端口转发请求          │  │
│  └─────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
```

### 核心模块

#### 1. 视频流处理
- **libscreen_casting.z.so**: 设备端录屏并 H.264 编码
- **Python Bridge**: gRPC → TCP 协议转换
- **H264VideoLayer**: 解析 NAL 单元，硬件解码渲染

#### 2. 输入注入
- **hdc uinput**: shell 命令注入，~100ms 延迟
- **自动节流**: 拖拽时以 10Hz 频率发送，避免命令堆积

#### 3. 设备发现
- USB: `hdc list targets` 轮询
- Wi-Fi: 局域网扫描 + 端口探测 (10178, 43101)
- 智能缓存: 记住成功连接的 IP，下次优先尝试

### 技术栈

- **Swift / SwiftUI**: macOS 原生界面
- **AVFoundation / VideoToolbox**: 硬件视频解码
- **hdc**: HarmonyOS 设备通信
- **Python gRPC**: 视频流转发桥接

## 项目文档

- [DESIGN.md](DESIGN.md) — 完整的架构设计和技术方案文档

## 分支说明

- **no-agent** (当前分支): 简化版本，仅使用 hdc 输入，无需部署 Agent
- **main** (完整版): 包含 HarmonyAgent 低延迟输入

如需低延迟输入体验，请切换到完整版分支。

## 已知限制

| 问题 | 原因 | 影响 |
|------|------|------|
| 拖拽有轻微跳跃 | hdc shell 进程启动延迟 ~100ms | 拖拽不够丝滑 |
| 锁屏/密码界面不可操作 | 安全窗口拒绝 hdc 注入 | 需手动解锁设备 |
| 边缘手势不完整 | uinput 无法触发系统面板 | 已改用 uitest，桌面可用 |
| 首帧延迟 | 关键帧间隔 2 秒 | 连接后短暂黑屏 |

## 贡献

欢迎提交 Issue 和 Pull Request！

## 许可证

MIT License

## 致谢

- 华为 DevEco Studio 和 hdc 工具
- OpenHarmony 开源社区
